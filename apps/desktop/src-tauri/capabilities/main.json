{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Dashboard permissions for local workspace management.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "global-shortcut:default",
    "dialog:allow-save"
  ]
}
