{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for local MVP development.",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "store:default",
    "global-shortcut:default"
  ]
}
