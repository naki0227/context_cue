{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Least-privilege permissions for read-only overlay windows.",
  "windows": ["overlay_top", "overlay_side"],
  "permissions": [
    "core:default"
  ]
}
